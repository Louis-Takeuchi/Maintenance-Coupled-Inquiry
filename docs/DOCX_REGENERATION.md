# DOCX regeneration note

The manuscript Markdown is the public-link canonical copy. Its Code and Data
Availability sections were updated to the GitHub repository URL. The supplied
DOCX was retained byte-for-byte because a safe, source-controlled DOCX build
pipeline was not included.

Before journal submission:

1. Confirm the Markdown wording and all unresolved author metadata.
2. Use the author's original Markdown-to-DOCX workflow or journal template.
3. Compare every result table, equation, reference, figure callout, and
   availability statement against the Markdown and frozen CSVs.
4. Render the DOCX and visually inspect every page.
5. Replace the snapshot only in a clearly reviewed commit; do not alter
   scientific values during format conversion.

