# Historical baseline

`constitutive_inquiry_mvp_v0_14` is a historical pre-unification baseline. It
did not generate the final confirmatory results and is not the canonical paper
implementation. See `docs/LEGACY_VERSIONS.md` before using it.

The cleaned v0.1–v0.14 archive is intentionally excluded from the Git tree and
planned as a versioned Release asset.

